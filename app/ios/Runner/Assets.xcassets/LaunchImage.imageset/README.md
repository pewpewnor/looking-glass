# Launch screen assets

Replace the image files in this directory to customize the launch screen.
You can also open `ios/Runner.xcworkspace`, select `Runner/Assets.xcassets`
in Xcode, and drop the replacement images into this asset set.
