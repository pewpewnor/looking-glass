name: looking_glass_app
description: "Object-finding app powered by a 3-model ONNX pipeline."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.0+2
  image_picker: ^1.1.2
  http: ^1.4.0
  shared_preferences: ^2.5.3
  go_router: ^14.8.1
  provider: ^6.1.5
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
