# looking-glass
A cross-platform object-finding app that uses a custom ONNX AI pipeline with three fine-tuned models for few-shot recognition, object localization, and depth estimation. Built with a Flutter mobile client and Go inference backend, it learns and locates user-defined objects in real-world environments.
